<Window x:Class="SekiroTool.Views.Windows.StartupOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Activate On Launch"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="460"
        Height="650"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <DataTemplate x:Key="StartupItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <CheckBox IsChecked="{Binding IsEnabled}"
                          IsEnabled="{Binding DataContext.IsEnabled, RelativeSource={RelativeSource AncestorType=Window}}"
                          Margin="0,0,8,0"
                          VerticalAlignment="Center" />
                <TextBlock Text="{Binding DisplayName}"
                           Width="180"
                           VerticalAlignment="Center" />
            </StackPanel>
        </DataTemplate>

        <Style x:Key="StartupItemsPanelStyle" TargetType="ItemsControl">
            <Setter Property="ItemTemplate" Value="{StaticResource StartupItemTemplate}" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StartupGroupHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,14,0,6" />
        </Style>

        <Style x:Key="StartupSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Margin" Value="0,14,0,6" />
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.5" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource TitleBarBrush}"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Activate On Launch"
                               Grid.Column="0"
                               Foreground="White"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0" />

                    <Button Content="✕"
                            Grid.Column="1"
                            Width="30"
                            Height="30"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0"
                          Content="Enable Activate On Launch / New Game"
                          IsChecked="{Binding IsEnabled}"
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"
                          ToolTip="When disabled, your selected options below are still saved but won't be activated on launch or New Game."
                          ToolTipService.InitialShowDelay="100" />

                <Border Grid.Row="1" BorderThickness="1" BorderBrush="#333" CornerRadius="3">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="10">
                            <TextBlock Text="Activate on Game Load" Style="{StaticResource StartupGroupHeaderStyle}"
                                       Margin="0,0,0,6" />

                            <TextBlock Text="Player" Style="{StaticResource StartupSectionHeaderStyle}" Margin="0,0,0,6" />
                            <ItemsControl ItemsSource="{Binding PlayerOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />

                            <TextBlock Text="Enemies" Style="{StaticResource StartupSectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{Binding EnemiesOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />

                            <TextBlock Text="Target" Style="{StaticResource StartupSectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{Binding TargetOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />

                            <TextBlock Text="Boss Skips" Style="{StaticResource StartupSectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{Binding BossSkipOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />

                            <TextBlock Text="Activate on New Game Cycle" Style="{StaticResource StartupGroupHeaderStyle}" />

                            <TextBlock Text="Player" Style="{StaticResource StartupSectionHeaderStyle}" Margin="0,0,0,6" />
                            <ItemsControl ItemsSource="{Binding NewGamePlayerOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />

                            <TextBlock Text="Event" Style="{StaticResource StartupSectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{Binding EventOptions}"
                                          Style="{StaticResource StartupItemsPanelStyle}" />
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
